apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-games
  labels:
    app: ms-games
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-games
  template:
    metadata:
      labels:
        app: ms-games      
      annotations:              
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"

    spec:
      containers:
      - name: ms-games
        image: jpedroduarte123/fiap-srv-games:latest
        ports:
        - containerPort: 8080
       
        envFrom:
        - secretRef:
            name: secrets-ms-games

        livenessProbe:
          httpGet:  
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 20

        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10

        resources: 
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "512Mi"